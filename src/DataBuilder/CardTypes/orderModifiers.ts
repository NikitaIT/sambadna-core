import { CardTypeRecord } from '../../models';

const data = {
    id: 'BkgnyNMmQ',
    name: 'Order Modifiers',
    reference: 'Order Modifier',
    color: 'goldenrod'
};

// id: string;
// name: string;
// reference: string;
// displayFormat: string;
// commands: string[];
// tagTypes: string[];
// subCardTypes: string[];

export default new CardTypeRecord(data);
