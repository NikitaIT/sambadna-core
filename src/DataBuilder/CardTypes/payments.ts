import { CardTypeRecord } from '../../models';

const data = {
    id: 'H1ZRqtCbm',
    name: 'Payments',
    reference: 'Payment',
    color: 'coral'
};

// id: string;
// name: string;
// reference: string;
// displayFormat: string;
// commands: string[];
// tagTypes: string[];
// subCardTypes: string[];

export default new CardTypeRecord(data);
