import { CardTypeRecord } from '../../models';

const data = {
    id: 'r1riomT5z',
    name: 'Tables',
    reference: 'Table'
};

// id: string;
// name: string;
// reference: string;
// displayFormat: string;
// commands: string[];
// tagTypes: string[];
// subCardTypes: string[];

export default new CardTypeRecord(data);
